<!DOCTYPE html>
<html lang="pt-BR" class="h-full">
  <head>
    <title>Smart Storage System</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">

    <script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio,container-queries"></script>
    <script>
      tailwind.config = {
        darkMode: 'class',
        theme: {
          extend: {
            fontFamily: {
              sans: ['"Plus Jakarta Sans"', 'Inter', 'system-ui', 'sans-serif']
            },
            colors: {
              surface: {
                50: '#f8fafc',
                100: '#f1f5f9',
                200: '#e2e8f0',
                700: '#1e293b',
                800: '#16213f',
                900: '#0f172a'
              },
              primary: {
                100: '#ede9fe',
                200: '#ddd6fe',
                300: '#c4b5fd',
                400: '#a78bfa',
                500: '#8b5cf6',
                600: '#7c3aed',
                700: '#6d28d9'
              }
            }
          }
        }
      }
    </script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-surface-50 text-ink-900 antialiased transition-colors duration-300 dark:bg-surface-900 dark:text-slate-200" data-controller="theme" data-action="turbo:load->theme#hydrate">
    <div class="flex min-h-screen flex-col items-center justify-center px-6 py-16">
      <div class="w-full max-w-lg rounded-3xl border border-white/20 bg-white/80 p-8 shadow-soft backdrop-blur dark:border-white/10 dark:bg-surface-800/80">
        <div class="mb-8 flex flex-col gap-1 text-center">
          <span class="text-xs uppercase tracking-[0.4em] text-ink-400 dark:text-slate-400">Smart Shelf</span>
          <h1 class="text-3xl font-semibold">Bem-vindo(a) de volta</h1>
          <p class="text-sm text-ink-500 dark:text-slate-300">Acesse sua despensa digital e mantenha toda a casa sincronizada.</p>
        </div>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
